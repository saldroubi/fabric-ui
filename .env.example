# URL where `fabric-ai --serve` is listening. Optional — defaults to
# http://127.0.0.1:8080 (fabric-ai's own default) if not set.
# Copy this file to .env and change it only if your fabric-ai --serve
# runs on a different host or port.
FABRIC_AI_URL=http://127.0.0.1:8080
